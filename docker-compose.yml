  version: '3'
    
  services:
    web:
      build: .
      image: delamio/visil_recognizer_project:2.0
      command: python manage.py runserver 0.0.0.0:8000
      ports:
        - "8000:8000"

